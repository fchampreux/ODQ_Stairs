  # Mailgun configuration  
    Mailgun.configure do |config|
    config.api_key = 'key-which-is-mine'
  end
        
    ActionMailer::Base.mailgun_settings = {
        api_key: 'key-which-is-mine',
        domain: 'keyman.MyDomain.net',
      }