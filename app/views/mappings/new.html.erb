<% provide(:title, (t('Managing') + ' ' + t('Mappings'))) %>

<% content_for :page_heading do %>
    <%= t('New') + ' ' + t('Mapping') %>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', @mappings_list %>
