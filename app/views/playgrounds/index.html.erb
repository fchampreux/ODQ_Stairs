<% provide(:title, (t('Managing') + ' ' + t('Playgrounds'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: playgrounds_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @playgrounds} %>

<%= will_paginate %>

<br />

<%= link_to 'New Playground', new_playground_path %>


