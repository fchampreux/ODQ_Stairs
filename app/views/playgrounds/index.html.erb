<% provide(:title, (t('Managing') + ' ' + t('Playgrounds'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_export", locals: {this_path: playgrounds_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @playgrounds} %>

<%= will_paginate %>

<br />

<%= link_to t('New') + ' ' +t('Playground'), new_playground_path %>


