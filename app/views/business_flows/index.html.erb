<% provide(:title, (t('ManagingBFs'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_export", locals: {this_path: business_flows_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @business_flows} %>

<%= will_paginate %>

<br />


