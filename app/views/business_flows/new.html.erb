<% provide(:title, (t('Managing') + ' ' + t('BFs'))) %>

<% content_for :page_heading do %>
    <%= t('New') %> <%= t('BF') %>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', business_flows_path %>
