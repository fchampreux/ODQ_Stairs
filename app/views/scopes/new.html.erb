<% provide(:title, (t('Managing') + ' ' + t('Scopes'))) %>

<% content_for :page_heading do %>
    <%= t('New') %> <%= t('Scope') %>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', scopes_path %>
