<% provide(:title, (t('Managing') + ' ' + t('Scopes'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: scopes_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @scopes} %>

<%= will_paginate %>

<br />


