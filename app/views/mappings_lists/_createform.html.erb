<%= form_for(@mappings_list) do |f| %>
  <% if @mappings_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mappings_list.errors.count, "error") %> prohibited this mappings_list from being saved:</h2>

      <ul>
      <% @mappings_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


      <div class="row">
        <div class="span2 text-right">Name:
        </div>
        <div class="span6 field"><%= f.text_field :name, :class => "span6" %>
        </div>
      </div>

      <div class="row">
        <div class="span2 text-right">Description:
        </div>
        <div class="span10 field"><%= f.text_area :description, :rows => 5, :class => "span8" %>
        </div>
      </div>

      <div class="row">
        <div class="span2 text-right">Source list:
        </div>
        <div class="span3 field"><%= f.collection_select :source_list_id, @lists_of_values, :id, :name %>
        </div>
        <div class="span2 text-right">Target list:
        </div>
        <div class="span3 field"><%= f.collection_select :target_list_id, @lists_of_values, :id, :name %>
        </div>
      </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
