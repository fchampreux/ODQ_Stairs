<%= form_for [@parent_territory, @territory] do |f| %>
  <% if @territory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@territory.errors.count, "error") %> prohibited this territory from being saved:</h2>

      <ul>
      <% @territory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render partial: "shared/object_definition_form", locals: {f: f} %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
