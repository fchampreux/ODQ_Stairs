<% provide(:title, (t('Managing') + ' ' + t('Territories'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: territories_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @territories} %>

<%= will_paginate %>

<br />


