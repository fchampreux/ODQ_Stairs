<% provide(:title, (t('Managing') + ' ' + t('Territories'))) %>

<% content_for :page_heading do %>
    <%= t('New') + ' ' + t('Territory') %>
<% end %>

<%= render 'form' %>

<%= link_to t('Back'), territories_path %>
