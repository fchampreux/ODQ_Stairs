<% provide(:title, (t('ManagingBRs'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: business_rules_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @business_rules} %>

<%= will_paginate %>

<br />
