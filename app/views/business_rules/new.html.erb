<% provide(:title, (t('Managing') + ' ' + t('BRs'))) %>

<% content_for :page_heading do %>
    <%= t('New') %> <%= t('BR') %>
<% end %>

<%= render 'form' %>

<%= link_to t('Back'), business_rules_path %>
