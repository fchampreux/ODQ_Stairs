<% provide(:title, (t('Managing') + ' ' + t('Organisations'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: organisations_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @organisations} %>

<%= will_paginate %>

<br />


