<% provide(:title, (t('Managing') + ' ' + t('Oranisations'))) %>

<% content_for :page_heading do %>
  <%= t('Organisation') %>: <%= @organisation.name %>
<% end %>

<%= render 'form' %>

<%= link_to t('Back'), @organisation %>
