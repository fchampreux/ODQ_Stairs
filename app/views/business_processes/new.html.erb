<% provide(:title, (t('Managing') + ' ' + t('BPs'))) %>

<% content_for :page_heading do %>
    <%= t('New') %> <%= t('BP') %>
<% end %>

<%= render 'form' %>

<%= link_to t('Back'), business_processes_path %>
