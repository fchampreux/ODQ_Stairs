<% provide(:title, (t('ManagingBPs'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: business_processes_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @business_processes} %>

<%= will_paginate %>

<br />

