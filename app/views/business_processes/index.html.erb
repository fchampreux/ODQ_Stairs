<% provide(:title, (t('ManagingBPs'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_export", locals: {this_path: business_processes_path, import_path: new_business_processes_import_path, import_message: "ImportBPs" } %>

<%= render partial: "shared/object_index", locals: {this_index: @business_processes} %>

<%= will_paginate %>

<br />

