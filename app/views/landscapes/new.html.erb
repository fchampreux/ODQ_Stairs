<% provide(:title, (t('Managing') + ' ' + t('Landscapes'))) %>

<% content_for :page_heading do %>
    <%= t('New') %> <%= t('Landscape') %>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', landscapes_path %>
