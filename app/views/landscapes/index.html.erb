<% provide(:title, (t('Managing') + ' ' + t('Landscapes'))) %>

<%= will_paginate %>

<%= render partial: "shared/simple_search", locals: {this_path: landscapes_path} %>

<%= render partial: "shared/object_index", locals: {this_index: @landscapes} %>

<%= will_paginate %>

<br />



