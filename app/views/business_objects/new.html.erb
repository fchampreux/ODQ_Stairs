<% provide(:title, (t('Managing') + ' ' + t('BOs'))) %>

<% content_for :page_heading do %>
    <%= t('New') %> <%= t('BO') %>
<% end %>

<%= render 'createform' %>

<%= link_to t('Back'), business_objects_path %>
